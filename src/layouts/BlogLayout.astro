---
import type { ImageMetadata } from "astro";
import Layout from "@/layouts/Layout.astro";
import Navbar from "@/components/sections/Navbar.astro";
import { getRelativeLocaleUrl } from "astro:i18n";

interface Props {
  title: string;
  description: string;
  socialTitle?: string;
  socialImage?: ImageMetadata;
  socialImageAlt?: string;
  pageType?: "website" | "article";
  publishedAt?: Date;
  updatedAt?: Date;
  author?: string;
}

import { t as tBase } from "@/lib/utils";
function t(key: string): string {return tBase(Astro, key)}

const {
  title,
  description,
  socialTitle,
  socialImage,
  socialImageAlt,
  pageType,
  publishedAt,
  updatedAt,
  author,
} = Astro.props;
---

<Layout
  title={title}
  description={description}
  socialTitle={socialTitle}
  socialImage={socialImage}
  socialImageAlt={socialImageAlt}
  pageType={pageType}
  publishedAt={publishedAt}
  updatedAt={updatedAt}
  author={author}
  animatedBackground={false}
>
  <Navbar />
  <slot />

  <footer class="relative z-10 border-t border-teal-200/70 dark:border-white/10">
    <div
      class="mx-auto flex max-w-280 flex-col gap-2 px-6 py-8 text-sm text-teal-700 dark:text-white/55 sm:flex-row sm:items-center sm:justify-between"
    >
      <p>{t("blog.footer")}</p>
      <a
        href={Astro.currentLocale
          ? getRelativeLocaleUrl(Astro.currentLocale, "/")
          : "/"}
        class="font-minecraft font-bold text-teal-950 transition-colors hover:text-emerald-600 dark:text-white dark:hover:text-emerald-400"
      >
        Steel<span class="text-emerald-600 dark:text-emerald-400">MC</span>
      </a>
    </div>
  </footer>
</Layout>
